apiVersion: v1
kind: Service
metadata:
  name: poko-portfolio
  labels:
    app.kubernetes.io/name: poko-portfolio
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: poko-portfolio
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
